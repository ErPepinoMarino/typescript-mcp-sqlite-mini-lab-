// Entry point del servidor MCP SQLite Lab
// Este archivo será el punto de entrada del servidor MCP
// que se construirá progresivamente en las siguientes fases.

export function main(): void {
  console.log("typescript-mcp-sqlite-lab - MCP Server");
}

main();
